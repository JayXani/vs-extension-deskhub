import vscode from 'vscode';
import { showMessage } from '../../Shared/ui/showMessage';

export class MaestroInitController {
    async execute(workspacePath: string, vscode: vscode.ExtensionContext) {
        try {

        } catch (e) {
            return;
        }
    }
}
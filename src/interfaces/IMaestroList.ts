export interface IMaestroList { 
    root: [
        {
            Chave: number,
            Nome: string,
            Descricao: string,
            Publicado: string,
            URLIcone: string,
            valor: number
        }
    ],
    total: string
}
import { MaestroConfig } from "../types/maestro-config"

const putMaestro = async (data_config: MaestroConfig) => {
    try{
        const urlMaestro = data_config.url_post
        const response = await fetch("")

    } catch (e) {

    } 
}